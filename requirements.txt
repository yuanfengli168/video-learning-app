# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
jinja2>=3.1.0
python-multipart>=0.0.20
# HTMX is loaded via CDN in templates — no pip package needed

# Database
sqlalchemy>=2.0.0

# Settings
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Auth
firebase-admin>=6.2.0

# Transcription
faster-whisper>=1.1.0

# MVP0.2 materials feature: PDF text extraction
pypdf>=5.0.0
# MVP0.2 followup: OCR fallback chain (macOS Vision via Swift CLI,
# Ollama vision via httpx, Tesseract via pytesseract + Pillow for
# rendering PNGs from PDF pages — we use Apple PDFKit via PyObjC for
# rendering instead of pdf2image + poppler so the deps stay minimal).
pytesseract>=0.3.10
Pillow>=10.0.0

# LLM
ollama>=0.4.4
httpx>=0.27.0,<0.28.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.25.0
pytest-cov>=6.0.0
